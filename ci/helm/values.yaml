# Default values for chatter.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

app_name: hello-boot-pks
docker_image: harbor.tan.springapps.io/chatter/hello-boot-pks:latest
replicas: 1
